server:
  fqdn: "qatarina.example.com"
  host: "localhost"
  port: 4597

auth:
  signup_enabled: true
  require_verified_accounts: true
  max_login_attempts: 3
  jwt_secret_key: "secret-key"
  jwt_issuer: "qatarina.example.com"
  jwt_expiry_timeout: 36000

db:
  host: "localhost"
  port: 5432
  username: "qatarina"
  password: "qatarina123"
  database: "qatarina"
  options: "sslmode=disable&connect_timeout=30"
  conn_max_lifetime: 3600

admin:
  email: "administrator@nndi.cloud"
  username: "admin"
  password: "base64-encoded"
  enabled: true

platform:
  securityKey: "some-security-key-base-64"
  allowAnonymousTestCaseSubmission: false
  disableSignup: true
